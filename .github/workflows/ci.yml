name: CI

on:
  push:
    branches:
      - dev

permissions:
  id-token: write

jobs:
  run-terraform:
    name: "Run terraform"
    runs-on: ubuntu-latest

    steps:
      - name: checkout
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: "arn:aws:iam::529928147423:role/terraform.role"
          aws-region: "us-east-2"
